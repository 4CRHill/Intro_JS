The code logs '510' instead of '15' because is concatenates the two values as if they were both 
strings using implicit type coercion. If either operand is a string and the other is not, 
JavaScript coerces the non-string operand to a string.
